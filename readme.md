# Setup backend

```json
{
  "name": "practice-mern-auth",
  "version": "1.0.0",
  "type": "module", // This will enable import export method
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "server": "nodemon backend/server.js"
  }
}
```
